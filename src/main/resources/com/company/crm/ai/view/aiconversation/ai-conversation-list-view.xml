<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<view xmlns="http://jmix.io/schema/flowui/view"
      title="msg://aiConversationListView.title"
      focusComponent="aiConversationsDataGrid">
    <data>
        <collection id="aiConversationsDc"
                    class="com.company.crm.ai.model.AiConversation">
            <fetchPlan extends="_base"/>
            <loader id="aiConversationsDl" readOnly="true">
                <query>
                    <![CDATA[select e from AiConversation e where e.firstMessageSent = true order by e.createdDate desc]]>
                </query>
            </loader>
        </collection>
    </data>
    <facets>
        <dataLoadCoordinator auto="true"/>
        <urlQueryParameters>
            <pagination component="pagination"/>
        </urlQueryParameters>
    </facets>
    <actions>
        <action id="selectAction" type="lookup_select"/>
        <action id="discardAction" type="lookup_discard"/>
    </actions>
    <layout>
        <hbox id="buttonsPanel" classNames="buttons-panel">
            <startSlot>
                <button id="createButton" action="aiConversationsDataGrid.createAction"/>
                <button id="editButton" action="aiConversationsDataGrid.editAction"/>
                <button id="removeButton" action="aiConversationsDataGrid.removeAction"/>
            </startSlot>
            <endSlot>
                <simplePagination id="pagination" dataLoader="aiConversationsDl"/>
            </endSlot>
        </hbox>
        <dataGrid id="aiConversationsDataGrid"
                  width="100%"
                  minHeight="20em"
                  dataContainer="aiConversationsDc"
                  columnReorderingAllowed="true">
            <actions>
                <action id="createAction" type="list_create" text="msg://aiConversationListView.createAction.text"
                        icon="vaadin:magic"/>
                <action id="editAction" type="list_edit" text="msg://aiConversationListView.editAction.text"
                        icon="vaadin:external-link"/>
                <action id="removeAction" type="list_remove"/>
            </actions>
            <columns resizable="true">
                <column property="title" width="60%"/>
                <column property="createdDate" width="40%"/>
            </columns>
        </dataGrid>
        <hbox id="lookupActions" visible="false">
            <button id="selectButton" action="selectAction"/>
            <button id="discardButton" action="discardAction"/>
        </hbox>
    </layout>
</view>
